import React from 'react';
import './Header.css';

function Header() {
  return (
    <div>Header</div>
    // In this section I think we will probably have a navbar along with
    // all the links to the separate pages, also maybe we include an icon
    // in the far left of the navbar
  );
}

export default Header;
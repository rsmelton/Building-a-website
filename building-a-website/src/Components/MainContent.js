import React from 'react';
import './MainContent.css';

function MainContent() {
  return (
    <div>MainContent</div>
    // In this section we can probably have the main content of the 
    // "home" page, so maybe like some pictures of the foods we "offer"
    // like in the example we saw
  );
}

export default MainContent;